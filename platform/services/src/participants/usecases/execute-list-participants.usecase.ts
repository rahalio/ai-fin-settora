/**
 * ExecuteListParticipants - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: participants
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListParticipants Use Case
 *
 * DDD: Application use case for list Participant.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListParticipantsInput, ListParticipantsOutput } from "../dto/participant.dto";
import type { ExecutionContextService, IdGeneratorService } from "@settora/services/_shared/index.js";
import type { ParticipantRepository } from "../ports";

export class ExecuteListParticipants {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly participant: ParticipantRepository
  ) {}

  async execute(input: ListParticipantsInput): Promise<ListParticipantsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.participantsId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.participant.listParticipants({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListParticipantsOutput;
  }
}
