/**
 * ExecuteOpenBreakCase - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: breaks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteOpenBreakCase Use Case
 *
 * DDD: Application use case for get Break.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { OpenBreakCaseInput, OpenBreakCaseOutput } from "../dto/break.dto";
import type { ExecutionContextService, IdGeneratorService } from "@settora/services/_shared/index.js";
import type { BreakRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteOpenBreakCase {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly breakRepo: BreakRepository
  ) {}

  async execute(input: OpenBreakCaseInput): Promise<OpenBreakCaseOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.breaksId();

    // All field names extracted from OpenAPI spec: assertionIds, conflictingFields, dealId, dueAt, ownerParticipantId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).dealId) {
      throw new ValidationError("dealId is required");
    }
    const dealId = (input as any).dealId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.breakRepo.openBreakCase({ dealId: dealId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${dealId}`);
    }

    return entity as OpenBreakCaseOutput;
  }
}
