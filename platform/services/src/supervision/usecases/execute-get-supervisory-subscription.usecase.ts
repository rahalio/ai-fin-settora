/**
 * ExecuteGetSupervisorySubscription - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: supervision
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetSupervisorySubscription Use Case
 *
 * DDD: Application use case for get Subscription.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetSupervisorySubscriptionInput, GetSupervisorySubscriptionOutput } from "../dto/subscription.dto";
import type { ExecutionContextService, IdGeneratorService } from "@settora/services/_shared/index.js";
import type { SubscriptionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetSupervisorySubscription {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly subscription: SubscriptionRepository
  ) {}

  async execute(input: GetSupervisorySubscriptionInput): Promise<GetSupervisorySubscriptionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.supervisionId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).subscriptionId) {
      throw new ValidationError("subscriptionId is required");
    }
    const subscriptionId = (input as any).subscriptionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.subscription.getSupervisorySubscription({ subscriptionId: subscriptionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${subscriptionId}`);
    }

    return entity as GetSupervisorySubscriptionOutput;
  }
}
