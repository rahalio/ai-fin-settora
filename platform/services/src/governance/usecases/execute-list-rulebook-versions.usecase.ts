/**
 * ExecuteListRulebookVersions - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: governance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListRulebookVersions Use Case
 *
 * DDD: Application use case for list Rulebook.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListRulebookVersionsInput, ListRulebookVersionsOutput } from "../dto/rulebook.dto";
import type { ExecutionContextService, IdGeneratorService } from "@settora/services/_shared/index.js";
import type { RulebookRepository } from "../ports";

export class ExecuteListRulebookVersions {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly rulebook: RulebookRepository
  ) {}

  async execute(input: ListRulebookVersionsInput): Promise<ListRulebookVersionsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.governanceId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.rulebook.listRulebookVersions({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListRulebookVersionsOutput;
  }
}
