/**
 * ExecuteCastGovernanceVote - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: governance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteCastGovernanceVote Use Case
 *
 * DDD: Application use case for get Vote.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { CastGovernanceVoteInput, CastGovernanceVoteOutput } from "../dto/vote.dto";
import type { ExecutionContextService, IdGeneratorService } from "@settora/services/_shared/index.js";
import type { VoteRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteCastGovernanceVote {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly vote: VoteRepository
  ) {}

  async execute(input: CastGovernanceVoteInput): Promise<CastGovernanceVoteOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.governanceId();

    // All field names extracted from OpenAPI spec: proposal, vote, voterParticipantId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).voterParticipantId) {
      throw new ValidationError("voterParticipantId is required");
    }
    const voterparticipantId = (input as any).voterParticipantId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.vote.castGovernanceVote({ voterParticipantId: voterparticipantId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${voterparticipantId}`);
    }

    return entity as CastGovernanceVoteOutput;
  }
}
