/**
 * GetSupervisorySubscription handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: supervision
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getSupervisorySubscription } from "@settora/api-server/domains/supervision/handlers";
import type { SupervisionDDDDependencies } from "@settora/api-server/domains/supervision";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@settora/tests/shared";
import { SUPERVISION_USE_CASES } from "../__factories__/use-cases.js";

describe("GetSupervisorySubscription", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SupervisionDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SupervisionDDDDependencies>(
      SUPERVISION_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetSupervisorySubscription, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { subscriptionId: entity.id };

    await getSupervisorySubscription(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetSupervisorySubscription.execute).toHaveBeenCalledWith(expect.objectContaining({ subscriptionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { subscriptionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetSupervisorySubscription, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getSupervisorySubscription(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
