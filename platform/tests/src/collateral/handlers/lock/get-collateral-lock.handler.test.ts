/**
 * GetCollateralLock handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: collateral
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getCollateralLock } from "@settora/api-server/domains/collateral/handlers";
import type { CollateralDDDDependencies } from "@settora/api-server/domains/collateral";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@settora/tests/shared";
import { COLLATERAL_USE_CASES } from "../__factories__/use-cases.js";

describe("GetCollateralLock", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CollateralDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CollateralDDDDependencies>(
      COLLATERAL_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetCollateralLock, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { lockId: entity.id };

    await getCollateralLock(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetCollateralLock.execute).toHaveBeenCalledWith(expect.objectContaining({ lockId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { lockId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetCollateralLock, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getCollateralLock(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
