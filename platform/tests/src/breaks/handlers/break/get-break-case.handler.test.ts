/**
 * GetBreakCase handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: breaks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getBreakCase } from "@settora/api-server/domains/breaks/handlers";
import type { BreaksDDDDependencies } from "@settora/api-server/domains/breaks";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@settora/tests/shared";
import { BREAKS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetBreakCase", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: BreaksDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<BreaksDDDDependencies>(
      BREAKS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetBreakCase, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { breakId: entity.id };

    await getBreakCase(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetBreakCase.execute).toHaveBeenCalledWith(expect.objectContaining({ breakId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { breakId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetBreakCase, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getBreakCase(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
