/**
 * GetAgreedState handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: deals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getAgreedState } from "@settora/api-server/domains/deals/handlers";
import type { DealsDDDDependencies } from "@settora/api-server/domains/deals";
import { createAgreedState as createAgreedStateEntity } from "@settora/factories/deals";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@settora/tests/shared";
import { DEALS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetAgreedState", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DealsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DealsDDDDependencies>(
      DEALS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createAgreedStateEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetAgreedState, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { dealId: entity.id };

    await getAgreedState(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetAgreedState.execute).toHaveBeenCalledWith(expect.objectContaining({ dealId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { dealId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetAgreedState, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getAgreedState(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
