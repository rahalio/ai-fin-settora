/**
 * Deals DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: deals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Deals Domain Module - Composition root
 *
 * DDD: Composition root for deals domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AgreedStateRepositoryAdapter,
  AssertionRepositoryAdapter,
  CorporateActionRepositoryAdapter,
  DealRepositoryAdapter,
  FreezeFieldRepositoryAdapter,
  MilestoneRepositoryAdapter,
  ProvenanceRepositoryAdapter,
} from "@settora/adapters/deals";
import { getIdGeneratorService } from "@settora/adapters";
import type { AdapterDynamoDBClient } from "@settora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAttestDocumentMilestone,
  ExecuteCreateTradeDeal,
  ExecuteGetAgreedState,
  ExecuteGetAssetProvenance,
  ExecuteGetTradeDeal,
  ExecuteListCorporateActionEvents,
  ExecuteListDealAssertions,
  ExecuteListDocumentMilestones,
  ExecuteListTradeDeals,
  ExecuteRecordCorporateActionEvent,
  ExecuteSubmitStateAssertion,
} from "@settora/services/deals/usecases";
// Ports (interfaces)
import type {
  AgreedStateRepository,
  AssertionRepository,
  CorporateActionRepository,
  DealRepository,
  FreezeFieldRepository,
  MilestoneRepository,
  ProvenanceRepository,
} from "@settora/services/deals/ports";

export interface DealsDomainModule {
  repos: {
    agreedStates: AgreedStateRepository;
    assertions: AssertionRepository;
    corporateActions: CorporateActionRepository;
    deals: DealRepository;
    freezeFields: FreezeFieldRepository;
    milestones: MilestoneRepository;
    provenances: ProvenanceRepository;
  };
  useCases: {
    agreedStates: {
      get: ExecuteGetAgreedState;
    };
    assertions: {
      create: ExecuteSubmitStateAssertion;
      list: ExecuteListDealAssertions;
    };
    corporateActions: {
      get: ExecuteRecordCorporateActionEvent;
      list: ExecuteListCorporateActionEvents;
    };
    deals: {
      create: ExecuteCreateTradeDeal;
      get: ExecuteGetTradeDeal;
      list: ExecuteListTradeDeals;
    };
    milestones: {
      get: ExecuteAttestDocumentMilestone;
      list: ExecuteListDocumentMilestones;
    };
    provenances: {
      get: ExecuteGetAssetProvenance;
    };
  };
}

export function buildDealsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): DealsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      agreedStates?: AgreedStateRepository;
      assertions?: AssertionRepository;
      corporateActions?: CorporateActionRepository;
      deals?: DealRepository;
      freezeFields?: FreezeFieldRepository;
      milestones?: MilestoneRepository;
      provenances?: ProvenanceRepository;
    } = {};

    obj.agreedStates = new AgreedStateRepositoryAdapter(dynamoClient);
    obj.assertions = new AssertionRepositoryAdapter(dynamoClient);
    obj.corporateActions = new CorporateActionRepositoryAdapter(dynamoClient);
    obj.deals = new DealRepositoryAdapter(dynamoClient);
    obj.freezeFields = new FreezeFieldRepositoryAdapter(dynamoClient);
    obj.milestones = new MilestoneRepositoryAdapter(dynamoClient);
    obj.provenances = new ProvenanceRepositoryAdapter(dynamoClient);
    return {
      agreedStates: obj.agreedStates!,
      assertions: obj.assertions!,
      corporateActions: obj.corporateActions!,
      deals: obj.deals!,
      freezeFields: obj.freezeFields!,
      milestones: obj.milestones!,
      provenances: obj.provenances!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    agreedStates: {
      get: new ExecuteGetAgreedState(executionContext, idGenerator, repos.agreedStates),
    },
    assertions: {
      create: new ExecuteSubmitStateAssertion(executionContext, idGenerator, repos.assertions),
      list: new ExecuteListDealAssertions(executionContext, idGenerator, repos.assertions),
    },
    corporateActions: {
      get: new ExecuteRecordCorporateActionEvent(executionContext, idGenerator, repos.corporateActions),
      list: new ExecuteListCorporateActionEvents(executionContext, idGenerator, repos.corporateActions),
    },
    deals: {
      create: new ExecuteCreateTradeDeal(executionContext, idGenerator, repos.deals),
      get: new ExecuteGetTradeDeal(executionContext, idGenerator, repos.deals),
      list: new ExecuteListTradeDeals(executionContext, idGenerator, repos.deals),
    },
    milestones: {
      get: new ExecuteAttestDocumentMilestone(executionContext, idGenerator, repos.milestones),
      list: new ExecuteListDocumentMilestones(executionContext, idGenerator, repos.milestones),
    },
    provenances: {
      get: new ExecuteGetAssetProvenance(executionContext, idGenerator, repos.provenances),
    },
  };
  return { repos, useCases: useCases as DealsDomainModule["useCases"] };
}
